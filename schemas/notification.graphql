scalar DateTime

type Notification {
  id: ID!
  token: String
  device: String
  os: String
  createdAt: DateTime
  updatedAt: DateTime
}

input NotificationInput {
  token: String!
  device: String
  os: String
}
